{
  "name": "mantle",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run packages/mantle/src/index.ts",
    "build:web": "bun run --cwd packages/mantle/web build",
    "build": "bun run build:web && bun build packages/mantle/src/index.ts --compile --target=${TARGET:-bun} --outfile=${OUTFILE:-dist/mantle}",
    "clean": "rm -f .*.bun-build"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  },
  "dependencies": {
    "hono": "^4.12.9",
    "zod": "^4.3.6"
  }
}
